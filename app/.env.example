# =============================================================================
# NEXT.JS FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in your actual values

# -----------------------------------------------------------------------------
# NEXTAUTH + OAUTH
# -----------------------------------------------------------------------------
# NextAuth.js configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_me_with_openssl_random

# GitHub OAuth (for user authentication)
GITHUB_CLIENT_ID=replace_me
GITHUB_CLIENT_SECRET=replace_me

# Bitbucket OAuth (optional)
BITBUCKET_CLIENT_ID=replace_me
BITBUCKET_CLIENT_SECRET=replace_me

# -----------------------------------------------------------------------------
# FIGMA OAUTH (Dev Mode / variables access)
# -----------------------------------------------------------------------------
FIGMA_CLIENT_ID=replace_me
FIGMA_CLIENT_SECRET=replace_me
FIGMA_REDIRECT_URI=http://localhost:3000/api/figma/callback

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# PostgreSQL connection string (Supabase, Neon, or local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pixelproof

# -----------------------------------------------------------------------------
# APP API (FastAPI) base
# -----------------------------------------------------------------------------
# API URL for frontend to connect to (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# Only variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Server-only secrets (like NEXTAUTH_SECRET) are only accessible in API routes
